{
  "name": "example",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "tsc && node src/index.js",
    "watch": "tsc -w & chokidar 'src' -c 'node src/index.js'",
    "clean": "rm tsconfig.tsbuildinfo && rm -rf src/**/*.js && yarn"
  },
  "dependencies": {
    "chokidar-cli": "^2.1.0",
    "node-mtmr": "../"
  },
  "devDependencies": {
    "@types/node": "^14.14.37",
    "typescript": "^4.2.4"
  }
}
